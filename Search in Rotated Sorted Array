# DSA
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        if target not in nums:
            return -1

        if len(nums) < 1:
            return -1

        for index, number in enumerate(nums):
            if number == target:
                return index
